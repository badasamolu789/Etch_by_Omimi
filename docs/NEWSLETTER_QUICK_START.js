// Public subscription (requires the database migration):
// await EtchSupabase.subscribeToNewsletter({ email: 'subscriber@example.com' });
//
// Newsletter campaigns are submitted from the authenticated admin newsletter form.
// Never put a Resend/service-role key in browser code or call the provider directly.
// See docs/NEWSLETTER_SETUP.md for deployment instructions.
